VIA_ENABLE  = yes
VIAL_ENABLE = yes
DYNAMIC_MACRO_ENABLE = yes
DYNAMIC_KEYMAP_LAYER_COUNT = 5

# RGB_MATRIX and the WS2812 vendor (PIO) driver come from keyboard.json.
#
# KEY_OVERRIDE_ENABLE is not set here: builddefs/build_vial.mk already does
# `KEY_OVERRIDE_ENABLE ?= yes`, so repeating it is pure noise.
